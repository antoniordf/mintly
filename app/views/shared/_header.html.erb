<div class="header">
  <div class="subheader">
    <%= link_to portfolio_path(current_user.portfolio) do %>
      <div class="logo" style="background-image: url(<%= image_path 'mintly.png' %>);">
      </div>
    <% end %>
    <div class="dropdown">
      <button class="dd dropdown-toggle" data-bs-toggle="dropdown" aria-expanded="false">
        <i class="fa-solid fa-user-tie"></i>
      </button>
      <ul class="dropdown-menu">
        <li><%= link_to "Log Out", destroy_user_session_path, data: { turbo_method: :delete }, class: "dropdown-item" %></li>
      </ul>
    </div>
  </div>
</div>
